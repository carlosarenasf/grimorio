# Frontend — el build de Vite INCRUSTA estas variables en el JS público.
# Por eso aquí SOLO puede haber datos públicos (URLs). NUNCA un secreto.

# URL pública del backend (de aquí salen REST y WebSocket).
VITE_API_URL=https://grimorio-backend.onrender.com
